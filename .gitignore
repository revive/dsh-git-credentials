# Dependencies
node_modules/

# Build output (node half + browser bundle; regenerate with `pnpm build`)
lib/

# Machine-specific generated tsconfig — regenerate for this checkout with
# `pnpm gen:tsconfig` (requires DSH_REPO)
tsconfig.json

# Logs and editor junk
*.log
.DS_Store

# Local publish smoke test artifacts (dsh plugin add verification)
.smoke-home/
.pnpm-smoke-store/

# Packed tarballs
*.tgz

# npm cache (local dry-run artifact)
.npm-cache/

# Editor/worktree state
.worx-state.json
